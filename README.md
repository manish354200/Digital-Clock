# Digital-Clock
A lightweight digital clock built with HTML, CSS, and JavaScript. It displays the current time in HH:MM:SS format and updates every second using JavaScript’s setInterval() function.
